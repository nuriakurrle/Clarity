# Gemeinsame Abhängigkeiten aller Agenten – wird sowohl vom Docker-Build
# (backend/Dockerfile) als auch lokal genutzt:
#   pip install -r backend/requirements.txt
fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.26.0
pydantic==2.5.0
crewai==0.1.0
python-dotenv==1.0.0
# Multipart-Uploads (Bilder zu Einträgen, Audio zu Transcribe)
python-multipart==0.0.9
# Speech-to-Text (agent_transcribe)
faster-whisper==1.1.1
# faster-whisper 1.1.1 importiert requests, deklariert es aber nicht selbst
requests==2.31.0
pytest==7.4.3
pytest-asyncio==0.21.1
# Zeitzonen-Daten für zoneinfo (python:slim bringt keine System-tzdata mit)
tzdata==2025.2
